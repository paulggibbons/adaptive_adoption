# AI Adoption Friction Suite — Build Sequence

1. Merge the in-development manifest entry and product specification.
2. Build the Audit React component and Astro route in `pg-advisory-astro`.
3. Verify mobile, autosave, print/PDF, incomplete-answer handling, and local-only privacy behavior.
4. Promote the manifest entry from `in-development` to `live` after the production route is verified.
5. Prepare the Maven course shell and upload-ready lesson assets.
6. Design the Mapper data contract using the same eight domain keys.
7. Build the Mapper, then the 90-Day Intervention Planner.
8. Add server-side response capture only through a versioned Supabase migration and explicit consent design.
