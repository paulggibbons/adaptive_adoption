<!-- AUTO-GENERATED by scripts/generate_tool_pages.py from data/tools.yml — DO NOT EDIT -->
# The Long List

Tools we want to build. Some are next-up (build-next), some have active work (in-development), some are intent without a date (planned), some are candidates we're evaluating (speculative).

{{ long_list_tools | length }} tools across the long list.

{% for status in ['build-next', 'in-development', 'planned', 'speculative'] %}
{% if tools_by_status[status] %}
## {{ status | replace('-', ' ') | title }}

{% for tool in tools_by_status[status] %}
### {{ tool.name }}

{{ (tool.short_description or "") | trim }}

*Domain:* {{ domain_display_names[tool.framework_mapping.domain] }}{% if tool.framework_mapping.pillar_name %} · *Pillar:* {{ tool.framework_mapping.pillar_name }}{% endif %} · *Type:* `{{ tool.type }}`

{% if tool.aspirational_notes %}
> {{ (tool.aspirational_notes or "") | trim }}

{% endif %}
---
{% endfor %}
{% endif %}
{% endfor %}
